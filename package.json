{
  "name": "redux-allow-deny",
  "version": "2.1.1",
  "description": "Redux middleware to execute a callback on action types using a allowlist or denylist approach",
  "main": "lib/index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "rm -rf lib && babel src -d lib",
    "prepublish": "npm run build",
    "start": "npm run build",
    "test": "mocha test/ --recursive --require @babel/register"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "keywords": [
    "redux",
    "middleware",
    "allowlist",
    "denylist"
  ],
  "repository": "https://github.com/rnsloan/redux-allow-deny.git",
  "author": "rnsloan1@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "@babel/register": "^7.10.1",
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "redux": "^4.0.5",
    "sinon": "^9.0.2"
  }
}
